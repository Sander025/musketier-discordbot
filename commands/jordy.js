const discord = require("discord.js");

module.exports.run = async (bot, message, args) => {

    return message.channel.send("Goedendag, Jordy is een hoerezoon die spent en de club en niks neukt");

}

module.exports.help = {
    name: "Jordy"
}